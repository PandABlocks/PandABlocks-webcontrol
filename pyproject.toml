[build-system]
# Pin versions compatible with dls-python3 for reproducible wheels
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PandABlocks-webcontrol"
description = "Web UI for PandABlocks"
requires-python = ">=3.10"
readme = "README.md"
authors = [{name = "Tom Cobb", email = "tom.cobb@diamond.ac.uk"}]
dynamic = ["version"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Natural Language :: English",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.6",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
]
dependencies = [
    "numpy",
    "tornado",
]

[project.urls]
Homepage = "https://github.com/PandABlocks/PandABlocks-webcontrol"

[project.optional-dependencies]
# For development tests/docs
dev = [
    "atomicwrites",
    "black",
    "coverage",
    "doc8",
    "flake8",
    "isort",
    "mock",
    "mypy",
    "nose",
    "pylint",
    "pytest",
    "pytest-cov",
    "pytest-black",
    "pytest-flake8",
    "pytest-mypy",
    "pytest-timeout",
    "sphinx",
    "sphinx-rtd-theme-github-versions",
    "typing",
    "types-mock",
]

[project.scripts]
panda-webcontrol = "malcolm.webcontrol:main"

[tool.setuptools]
zip-safe = false
include-package-data = true

[tool.setuptools.packages.find]
# Don't include our tests directory in the distribution
exclude = ["tests"]

[tool.setuptools.package-data]
malcolm = [
    "modules/*/*/*.svg",
    "modules/*/*/*/*.json",
    "modules/web/www/*",
    "modules/web/www/static/css/*",
    "modules/web/www/static/js/*",
    "modules/web/www/static/media/*",
]

[tool.mypy]
# Ignore missing stubs for modules we use
ignore_missing_imports = true

[tool.isort]
profile = "black"
float_to_top = true
skip = ["setup.py", "conf.py", "build"]

[tool.pytest.ini_options]
# Run pytest with all our checkers, and don't spam us with massive tracebacks on error
addopts = "--tb=native -vv --black --mypy --doctest-modules"

[tool.ruff]
src = ["python", "tests"]
line-length = 88
lint.select = [
    "B",  # flake8-bugbear - https://docs.astral.sh/ruff/rules/#flake8-bugbear-b
    "C4", # flake8-comprehensions - https://docs.astral.sh/ruff/rules/#flake8-comprehensions-c4
    "E",  # pycodestyle errors - https://docs.astral.sh/ruff/rules/#error-e
    "F",  # pyflakes rules - https://docs.astral.sh/ruff/rules/#pyflakes-f
    "W",  # pycodestyle warnings - https://docs.astral.sh/ruff/rules/#warning-w
    "I",  # isort - https://docs.astral.sh/ruff/rules/#isort-i
    "UP", # pyupgrade - https://docs.astral.sh/ruff/rules/#pyupgrade-up
]
